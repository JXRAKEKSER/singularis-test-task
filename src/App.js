/*Импорты в файлах JS и его расширений указываются в порядке: импорты внешних библиотек -> компоненты этого проекта -> кастомные решения*/
import SalaryPerHour from './components/SalaryPerHour';

function App() {
  
  return (
    <>
      <SalaryPerHour />
    </>
  );
}

export default App;
